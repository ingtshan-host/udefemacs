# -*- mode: snippet -*-
# name: new-emacs-lisp-file
# key: ne
# --
;;; `(cond ((buffer-file-name) (file-name-nondirectory (buffer-file-name))) (t ""))` -*- lexical-binding: t; -*-

;;; Commentary:

;; $1

;;; Code:
$0
;; (provide '`(cond ((buffer-file-name) (file-name-base (buffer-file-name))) (t ""))`)
;;; `(cond ((buffer-file-name) (file-name-nondirectory (buffer-file-name))) (t ""))` ends here